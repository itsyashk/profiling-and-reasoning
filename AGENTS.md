you are never allowed to delete any files.

## Assignment Source of Truth

- Always check [hw2.pdf](hw2.pdf) before implementing or answering questions about a specific homework section.
- When asked to work on a section, read the raw PDF text with tools first instead of relying on memory or summaries.
- Use the PDF wording as the source of truth for requirements, expected behavior, constraints, and writeup prompts.
- If the PDF text extraction is unclear, inspect the relevant page visually or ask for clarification before making assumptions.

## Implementation Workflow

- Inspect the existing code and tests before editing.
- Keep changes narrowly scoped to the requested section.
- Prefer the repository's existing APIs, style, and helper functions over new patterns.
- Run the most relevant tests or commands after changes when feasible.
- Do not revert unrelated user changes.
- Always include the final commands the user should run, including `cd` when useful.

## Writeup Workflow

- Check the relevant prompt in [hw2.pdf](hw2.pdf) before editing `writeup.tex`.
- Keep writeup answers aligned with the implemented code and measured results.
- Preserve the existing LaTeX style unless a change is needed for correctness.